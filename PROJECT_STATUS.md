# Narchives - Project Status

## Overview

Android app (Kotlin/Jetpack Compose) for browsing web page archives published via Nostr (kind 30041 events) and stored on Blossom media servers. Functions like an RSS reader for WACZ web archives.

**Spec file:** `nostr-archive-reader-spec.md`

---

## Architecture Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Nostr library | **Amethyst Quartz** (`com.vitorpamplona.quartz:quartz-android:1.06.0`) | Pure Kotlin, battle-tested (powers Amethyst), includes relay client, bech32, sig verification, Blossom event types. No need to roll our own. |
| WebSocket impl | **BasicOkHttpWebSocket** (bundled in Quartz) | Already in the library's jvmAndroid source set. No custom adapter needed. |
| Dev environment | **Nix flake + direnv** | JDK 17, Android SDK 35, Gradle 8.11.1, nak 0.17.4. Fully reproducible. |
| DI | **Manual (AppContainer)** | Simple, no Hilt/Dagger. Spec recommends this. |
| Package name | `com.narchives.reader` | App name: Narchives |

---

## Build Phases & Status

### Phase 0: Nix Dev Environment -- DONE
- [x] `flake.nix` with androidenv, JDK 17, Gradle, nak
- [x] `.envrc` with `use flake`
- [x] Verified: `java -version`, `$ANDROID_HOME`, `nak --version`, `gradle --version` all work
- [x] `local.properties` auto-generated by shellHook

### Phase 1: Android Project Scaffold -- DONE
- [x] `settings.gradle.kts`, root `build.gradle.kts`
- [x] `gradle.properties`, `.gitignore`
- [x] Gradle wrapper (8.11.1)
- [x] `app/build.gradle.kts` with all dependencies
- [x] `AndroidManifest.xml`
- [x] `NarchivesApp.kt`, `MainActivity.kt`, `AppContainer.kt`
- [x] `proguard-rules.pro`
- [x] `themes.xml`, `strings.xml`

### Phase 2: Data Layer -- DONE
- [x] Room entities: `ArchiveEventEntity`, `RelayEntity`, `ProfileEntity`, `SavedArchiveEntity`
- [x] DAOs: `ArchiveEventDao`, `RelayDao`, `ProfileDao`, `SavedArchiveDao` (all with Flow observers)
- [x] `NarchivesDatabase` (Room database class)
- [x] `UserPreferences` (DataStore - theme, reader font, cache limit)

### Phase 3: Nostr Client Layer -- DONE
- [x] `NostrClient` - thin wrapper around Quartz's `NostrClient` with `SharedFlow<RelayEvent>` API
- [x] `EventMapper` - maps Quartz `Event` objects to Room entities (kind 30041, kind 0, kind 10063)
- Uses Quartz's built-in: event parsing, signature verification, bech32, filter construction

### Phase 4: Blossom Client Layer -- DONE
- [x] `BlossomClient` - HEAD checks, downloads with SHA-256 verification, URL resolution chain (direct -> author servers -> fallback servers)
- [x] `BlobInfo` data class

### Phase 5: Domain Layer (Repositories) -- DONE
- [x] `ArchiveRepository` - subscribes to relays, stores events, resolves WACZ URLs
- [x] `ProfileRepository` - fetches/caches profiles with 24h TTL
- [x] `RelayRepository` - CRUD, seed defaults on first launch

### Phase 6: Replay & Reader Engines -- IN PROGRESS
- [ ] `ReplayServer` (NanoHTTPD - serves assets + proxies WACZ with range requests)
- [ ] `ReaderModeExtractor` (WACZ ZIP -> WARC parse -> Readability4J)
- [ ] Bundle ReplayWeb.page assets (`ui.js`, `sw.js`, `index.html`)
- [ ] `WaczDownloadWorker` (WorkManager)

### Phase 7: UI Layer -- NOT STARTED
- [ ] Theme (`Color.kt`, `Type.kt`, `Theme.kt`)
- [ ] Navigation (`NarchivesNavGraph.kt` - bottom nav + full-screen routes)
- [ ] Shared components (`ArchiveCard`, `UrlFilterBar`, `ProfileHeader`, etc.)
- [ ] FeedScreen + FeedViewModel
- [ ] RelayBrowserScreen + RelayBrowserViewModel
- [ ] ProfileScreen + ProfileViewModel
- [ ] ArchiveViewerScreen + ArchiveViewerViewModel (WebView + ReplayServer)
- [ ] ReaderModeScreen + ReaderModeViewModel
- [ ] SavedScreen + SavedViewModel
- [ ] SettingsScreen + SettingsViewModel

### Phase 8: Wire Together + Verify Build -- NOT STARTED
- [ ] Seed default relays in `NarchivesApp.onCreate()`
- [ ] Verify `./gradlew assembleDebug` compiles clean
- [ ] Download ReplayWeb.page assets into `assets/replay/`
- [ ] Generate test keys with `nak`
- [ ] Test against live relays (kind 30041 on `wss://relay.nostr.band`)

---

## Key Files

```
flake.nix                           # Nix dev environment
.envrc                              # direnv integration
settings.gradle.kts                 # Gradle project config
app/build.gradle.kts                # Dependencies (Quartz, Room, Compose, NanoHTTPD, etc.)
app/src/main/AndroidManifest.xml

# Core app
app/.../reader/NarchivesApp.kt
app/.../reader/MainActivity.kt
app/.../reader/di/AppContainer.kt

# Data layer
app/.../data/local/NarchivesDatabase.kt
app/.../data/local/entity/*.kt      # 4 Room entities
app/.../data/local/dao/*.kt         # 4 DAOs

# Nostr + Blossom
app/.../data/remote/nostr/NostrClient.kt
app/.../data/remote/nostr/EventMapper.kt
app/.../data/remote/blossom/BlossomClient.kt

# Repositories
app/.../data/repository/ArchiveRepository.kt
app/.../data/repository/ProfileRepository.kt
app/.../data/repository/RelayRepository.kt

# Preferences
app/.../data/preferences/UserPreferences.kt
```

---

## What's Next (Resume Here)

1. **Finish Phase 6**: Write `ReplayServer.kt`, `ReaderModeExtractor.kt`, download ReplayWeb.page assets, write `WaczDownloadWorker.kt`
2. **Phase 7**: Build all UI (theme -> nav -> components -> screens)
3. **Phase 8**: Wire together, get a clean build, test on device

---

## Dependencies (from app/build.gradle.kts)

- Quartz (Nostr): `com.vitorpamplona.quartz:quartz-android:1.06.0`
- OkHttp: `4.12.0`
- Room: `2.6.1`
- Compose BOM: `2024.12.01`
- Navigation Compose: `2.8.5`
- Coil (images): `2.7.0`
- Readability4J: `1.0.8`
- NanoHTTPD: `2.3.1`
- WorkManager: `2.9.1`
- DataStore: `1.1.1`

## Test Tools

- `nak` (Nostr Army Knife) v0.17.4 — available in Nix shell for key generation and relay queries
